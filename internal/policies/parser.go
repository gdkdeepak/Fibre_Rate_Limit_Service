package policies

// ParsePolicy is a placeholder for future rule parsing.
func ParsePolicy(raw string) map[string]string {
	// simple stub parsing for now
	return map[string]string{
		"raw": raw,
	}
}
